섹션 2_1.자바스크립트 DOM이란 ?

JS = html문서를 조작하기 위해 만들어진 문서 

문서를 객채화 했다 = Document Object Model 
                    (문서    객채    모델)

DOM은 트리구조를 가지고있음

Document 객체 = 웹 영역을 제어하는 객체 
                웹페이지 자체를 의미하면서 
                DOM트리 최상위 객체
Document.getRootNode() 최상위 노드 

document.childNodes = 자식노드 반환 

결국 DOM은 문서 객체 모델로 브라우저에서 HTML을 javascripts로 제어할수있게 하는 API임 

섹션 2_2 :HTML요소 선택하기 

진행순서
1.HTML요소 선택
2.HTML 요소 속성 조작 
3.HTML 요소 스타일 수정

HTML 요소 가저오는 2가지방법
get메소드 , query로 시작하는 함수를 이용해서

섹션 2_5 데이터 속성
데이터 속성 
HTML에 임의의 데이터를 추가하고 싶을때 사용할수있다
브라우저는 이 속성을 완전하게 무시하므로 JS에서 쉽게 요소에 관한 정보를
읽거나 수정할수있음.

HTML 요소의 dataset프로퍼티로 (data-)속성을 읽거나 쓸수있다

data속성은 data-(임의설정)

접근은 dataset.(임의설정)



섹션 2_6 이벤트 등록하기
inline(인라인)
이벤트 대상의 HTML 속성으로 지정this를 통해 이벤트가 발생한 대상 요소를 참조할 수 있다.

프로퍼티 리스터(Property Listener)

AddEventListener()

이벤트 제거하기 
removeEventListner()

Event 객체
발생한 이벤트에 관련된 다양한 속성과 메서드를 가지고 있다.

이벤트 종류 MDN 화면에서 찾아쓰세용~~~ 구글링해쓰세용~~~ gpt 쓰세용~~~


3장

색션 3 변수 상수
  ※자바 스크립트 변수 let 상수 const
  ※let보다 const 사용이 좋음
  ※단 값이 변하면 let 쓰는건 너무 당연함

스코프(Scpoe)
  영역 변수나 함수를 사용 할 수 있는 영역
  블럭을 지정하지 않고 변수를 지정하면 전역 Scpoe
  전역 스코프에 설정된 변수를 전역 Scope라고함.
  블럭에서 선언된 변수는 블럭에서만 사용가능함

식별자

식별자 이름 정의 규칙
  변수명 앞에는 특수문자($,_)가능
  변수명은 예약어 사용불가능

네이밍 규칙
  카멜케이스 : 소문자 시작 단어바뀔때 대문자 낙타혹
  스네이크 케이스 : 단어 바뀔떄마다 _
  케밥케이스 : 단어가 바뀔떄마다 dash(-)
  파스칼케이스 : 단어 바뀔떄 마다 대문자

var 키워드 
  이거 쓰지말래


호이스팅
  끌어올려진다. 
  이거 몰라도 코딩하는데 의미없는데 면접질문
  호이스팅은 코드가 실행되기 전 변수나 함수가 코드 최상단으로 끌어올려짐.
  브라우저에서 자바 스크립트를 실행전에 funtion을 최상단으로 끌어올림 : function 호이스팅
  var로 선언한 변수는 var 호이스팅이 발생함
